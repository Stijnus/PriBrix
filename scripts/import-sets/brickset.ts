export function getBricksetEnrichmentStub() {
  return {
    implemented: false,
    reason: 'Brickset MSRP enrichment is intentionally deferred until after the Rebrickable import flow is stable.',
  };
}
